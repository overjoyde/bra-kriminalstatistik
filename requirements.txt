# Direkta beroenden (intervall). Håll i synk med pyproject.toml – kontrolleras av tests/test_packaging.py.
# Reproducerbar installation (setup-skripten, CI, schemalagda körningar): requirements.lock.
# Uppdatera låsfilen efter ändring här:
#   uv pip compile requirements.txt --universal --python-version 3.10 -o requirements.lock
#
# Hämtning (SOL + bra.se + DOMstat) kräver bara Pythons standardbibliotek.
# truststore gör att Python använder operativsystemets certifikatlager (rekommenderas på macOS/Windows).
truststore>=0.9,<1
# Tolkning av Excel-tabeller och dashboards
pandas>=2.0,<4
numpy>=1.24,<3
openpyxl>=3.1,<4
xlrd>=2.0,<3
# Exempelgrafer (scripts/make_charts.py)
matplotlib>=3.7,<4
